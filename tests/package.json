{
  "name": "tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "scenarios": "playwright test scenarios",
    "rules": "playwright test rules"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
