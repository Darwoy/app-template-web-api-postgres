host: template.127.0.0.1.sslip.io

images:
  web:
    repository: ghcr.io/koosha-t/app-template-web-api-postgres/web
    tag: "0000000"
  api:
    repository: ghcr.io/koosha-t/app-template-web-api-postgres/api
    tag: "0000000"

replicas: 1

resources:
  web:
    requests: { cpu: 50m, memory: 128Mi }
    limits: { cpu: 500m, memory: 256Mi }
  api:
    requests: { cpu: 50m, memory: 128Mi }
    limits: { cpu: 500m, memory: 256Mi }
  postgres:
    requests: { cpu: 100m, memory: 256Mi }
    limits: { cpu: "1", memory: 512Mi }

postgres:
  storage: 1Gi
  user: app
  password: app-local-only
  database: app
