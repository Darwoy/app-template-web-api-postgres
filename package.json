{
  "name": "app-template-web-api-postgres",
  "private": true,
  "workspaces": [
    "api",
    "web",
    "tests"
  ],
  "scripts": {
    "test": "npm run test --workspace=api && npm run test --workspace=web",
    "scenarios": "npm run scenarios --workspace=tests",
    "rules": "npm run rules --workspace=tests",
    "lock:packages": "bash scripts/lock-packages.sh"
  },
  "engines": {
    "node": ">=22"
  }
}
